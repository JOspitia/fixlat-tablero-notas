export default function DashboardPage() {
    return (
        <div className="p-8">
            <h1 className="text-2xl font-bold text-gray-800 mb-2">Dashboard</h1>
            <p className="text-gray-600">
                Vista del dashboard de métricas (HU-03). Esta página es un placeholder hasta
                que se implemente el consumo de <code className="bg-gray-100 px-1 rounded">GET /api/metrics</code>.
            </p>
        </div>
    );
}
