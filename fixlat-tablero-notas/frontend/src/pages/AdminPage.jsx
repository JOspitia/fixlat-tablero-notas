export default function AdminPage() {
    return (
        <div className="p-8">
            <h1 className="text-2xl font-bold text-gray-800 mb-2">Administración de usuarios</h1>
            <p className="text-gray-600">
                Vista de administración de usuarios (HU-04). Esta página es un placeholder hasta
                que se implemente el CRUD completo de usuarios con la invariante del último admin activo.
            </p>
        </div>
    );
}
